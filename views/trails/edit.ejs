<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Trail</title>
</head>
<body>
    <h1>Edit Trail</h1>
    <form action="/trails/<%= trail._id>?_method=PUT" method="POST">
        <label for="name">Name:</label>
        <input type="text" id="name" name="trail[name]" value="<%= trail.name %>" required>
        <label for="location">Location:</label>
        <input type="text" id="location" name="trail[location]" value="<%= trail.location %>" required>
        <label for="difficulty">Difficulty:</label>
        <input type="text" id="difficulty" name="trail[difficulty]" value="<%= trail.difficulty %>" required>
        <label for="length">Length (mi):</label>
        <input type="number" id="length" name="trail[length]" value="<%= trial.length %>" required>
        <label for="description">Description:</label>
        <textarea id="description" name="trail[description]" required><%= trail.description %></textarea>
            <button type="submit">Update Trail</button>
    </form>
</body>
</html>